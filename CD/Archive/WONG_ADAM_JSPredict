function findMax(arr){
    var max = 0;
    for (x=0; x<(arr.length);x++){
        if (arr[x]>max){
        max = arr[x];
        }
    }
    return max;
}

arr = [1, 4, 5, 6, 7, 9, 10];
console.log(max);